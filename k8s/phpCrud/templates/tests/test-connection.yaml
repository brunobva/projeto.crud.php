apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "phpCrud.fullname" . }}-test-connection"
  labels:
    {{- include "phpCrud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "phpCrud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
